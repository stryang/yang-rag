# 代码规范与最佳实践

## 1. 命名规范

### 1.1 变量命名
```python
# Good
user_name = "张三"
is_active = True
total_count = 100

# Bad
n = "张三"      # 语义不明确
flag = True     # 命名不清晰
cnt = 100       # 缩写不通用
```

### 1.2 函数命名
```python
# Good
def get_user_by_id(user_id: int) -> User:
    """根据ID获取用户信息"""
    pass

def calculate_total_price(items: list) -> float:
    """计算总价"""
    pass

# Bad
def get(id):        # 缺少类型提示和文档
    pass

def calc(items):     # 缩写不清晰
    pass
```

### 1.3 类命名
```python
# Good
class UserService:
    """用户服务类"""
    pass

class OrderRepository:
    """订单仓储类"""
    pass

# Bad
class service:      # 驼峰命名
    pass

class order_repo:   # 下划线命名
    pass
```

## 2. 代码组织

### 2.1 模块结构
```
src/
├── core/           # 核心模块
├── models/         # 数据模型
├── services/       # 业务逻辑
├── repositories/  # 数据访问
├── api/            # API接口
└── utils/          # 工具函数
```

### 2.2 导入顺序
```python
# 1. 标准库
import os
import json
from typing import Optional

# 2. 第三方库
from fastapi import FastAPI
from pydantic import BaseModel

# 3. 本地模块
from .core.config import settings
from .models.user import User
```

## 3. 错误处理

### 3.1 异常分类
```python
class AppException(Exception):
    """应用异常基类"""
    def __init__(self, message: str, code: str = "APP_ERROR"):
        self.message = message
        self.code = code
        super().__init__(self.message)


class ValidationError(AppException):
    """验证错误"""
    def __init__(self, message: str):
        super().__init__(message, "VALIDATION_ERROR")


class NotFoundError(AppException):
    """资源未找到"""
    def __init__(self, resource: str, resource_id: str):
        message = f"{resource} with id {resource_id} not found"
        super().__init__(message, "NOT_FOUND")
```

### 3.2 异常处理
```python
# Good - 明确的错误处理
try:
    user = user_service.get_user(user_id)
except NotFoundError as e:
    raise HTTPException(status_code=404, detail=str(e))
except ValidationError as e:
    raise HTTPException(status_code=400, detail=str(e))

# Bad - 捕获所有异常
try:
    user = user_service.get_user(user_id)
except Exception:
    pass
```

## 4. 异步编程

### 4.1 异步函数定义
```python
# Good
async def fetch_user(user_id: int) -> User:
    """异步获取用户"""
    return await user_repository.find_by_id(user_id)

# Bad - 混用同步异步
def fetch_user(user_id: int) -> User:
    return user_repository.find_by_id(user_id)  # 同步调用
```

### 4.2 并发处理
```python
# Good - 并发执行
async def get_dashboard_data(user_id: int) -> dict:
    user_task = fetch_user(user_id)
    orders_task = fetch_orders(user_id)
    stats_task = fetch_stats(user_id)

    user, orders, stats = await asyncio.gather(
        user_task, orders_task, stats_task
    )

    return {"user": user, "orders": orders, "stats": stats}
```

## 5. 日志规范

```python
import logging

logger = logging.getLogger(__name__)

# Good - 结构化日志
logger.info("User created", extra={
    "user_id": user.id,
    "action": "create",
    "duration_ms": 150
})

# Bad - 字符串拼接
logger.info(f"User {user.id} created in {duration}ms")
```

## 6. 测试规范

### 6.1 测试命名
```python
# Good
class TestUserService:
    def test_get_user_by_id_returns_user(self):
        pass

    def test_get_user_by_id_raises_not_found(self):
        pass

# Bad
class TestUser:
    def test1(self):
        pass

    def test_get(self):
        pass
```

### 6.2 测试结构
```python
def test_calculate_total_price(self):
    # Arrange - 准备测试数据
    items = [
        Item(name="商品1", price=100, quantity=2),
        Item(name="商品2", price=50, quantity=1)
    ]

    # Act - 执行被测方法
    total = calculate_total_price(items)

    # Assert - 验证结果
    assert total == 250
```
